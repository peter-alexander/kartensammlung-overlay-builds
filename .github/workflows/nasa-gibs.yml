name: Build NASA GIBS catalog

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'nasa-gibs/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/scripts/main-ftp-upload-file.sh'
      - '.github/workflows/nasa-gibs.yml'
  push:
    branches:
      - main
    paths:
      - 'nasa-gibs/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/scripts/main-ftp-upload-file.sh'
      - '.github/workflows/nasa-gibs.yml'
  schedule:
    - cron: '43 4 * * *'

concurrency:
  group: nasa-gibs-catalog
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      MAIN_FTP_HOST: ${{ secrets.MAIN_FTP_HOST }}
      MAIN_FTP_USER: ${{ secrets.MAIN_FTP_USER }}
      MAIN_FTP_PASSWORD: ${{ secrets.MAIN_FTP_PASSWORD }}
      MAIN_REMOTE_ROOT: ${{ vars.MAIN_REMOTE_ROOT }}
      CURRENT_CATALOG_URL: https://fahrrad.lima-city.de/Maps/NasaGibs/NasaGibsLayerCatalog.min.json

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'
          cache: npm

      - name: Install dependencies
        run: npm ci --no-fund --no-audit

      - name: Build complete NASA GIBS catalog
        run: node nasa-gibs/build.mjs

      - name: Check whether production catalog changed
        id: changed
        run: |
          if curl -fsS --retry 3 --retry-delay 2 -o /tmp/current-gibs.json "$CURRENT_CATALOG_URL" \
            && cmp -s /tmp/current-gibs.json nasa-gibs/build/NasaGibsLayerCatalog.min.json; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "NASA GIBS catalog unchanged."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "NASA GIBS catalog changed or current production file is unavailable."
          fi

      - name: Deploy catalog to Lima
        if: github.event_name != 'pull_request' && steps.changed.outputs.changed == 'true'
        run: |
          test -n "$MAIN_REMOTE_ROOT"
          chmod +x .github/scripts/main-ftp-upload-file.sh
          .github/scripts/main-ftp-upload-file.sh \
            nasa-gibs/build/NasaGibsLayerCatalog.min.json \
            "${MAIN_REMOTE_ROOT%/}/NasaGibs/NasaGibsLayerCatalog.min.json"
